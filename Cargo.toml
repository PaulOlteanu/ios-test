[package]
name = "ios-test"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "cdylib", "staticlib"]

[dependencies]
tokio = { version = "1.40.0", features = ["full"] }
uniffi = "0.28.1"

[build-dependencies]
uniffi = { version = "0.28", features = ["build"] }
